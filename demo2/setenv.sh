#!/bin/sh
# Execute this script with `source ./setenv.sh`.
# `source` will run commands in current terminal.
export POSTGRES_HOST=localhost
export POSTGRES_PORT=5432
export POSTGRES_USER=postgres
export POSTGRES_DB=websensor
export POSTGRES_PASSWORD=secret